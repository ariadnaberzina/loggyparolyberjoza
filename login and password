#include <iostream>
using namespace std;
int main()
{
	/*int age = -20;
	if (age > 0) {
		cout << "ok" << endl;
	}
	else cout << "wrong" << endl;
	cout << "exit";
	*/
/*	int dog = 30, cat = 12;
	if (dog >= 0 && dog <= 30) { cout << "dogs age is ok" << endl; }
	else cout << "wrong dogs age" << endl;
	if (cat > 0 && cat <= 20) { cout << "cats age is ok" << endl; }
	else cout << "wrong dogs age" << endl; */

	int age = 15;
	string loggy = "Berjoza";
	cout << "------------------------" << endl;
	string loggy_put; cout << "Login :     ";
	cin >> loggy_put;
	string parole = "berjoza29";
	string parole_put;cout << "Parole :     ";
	cin >> parole_put;
	cout << "------------------------" << endl;
	if (parole_put == parole&& loggy_put == loggy)
	{
		cout << "ok" <<endl << age;
	}
	else cout << "bad boy";

}
